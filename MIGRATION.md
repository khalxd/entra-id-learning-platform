# File split

The original single-file app was separated into:

- `index.html` — page structure
- `styles.css` — visual styling and responsive layout
- `labs.js` — all lab content/data
- `app.js` — navigation, progress, notes, export, search, theme, and event handling

Keep all four files in the repository root so the relative links continue to work on GitHub Pages.
